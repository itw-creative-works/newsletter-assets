LLM agents can burn through tokens fast when every step replays the full context and defaults to the most expensive model. The teams keeping costs under control are using routing, smaller specialized checks, and orchestration that matches the model to the task. That same pattern applies beyond code agents, including document workflows and OCR pipelines where not every step needs the same amount of compute.